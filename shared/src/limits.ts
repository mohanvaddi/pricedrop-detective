export const MAX_CUSTOM_LISTS = 3;

// Max products a single user can track. Kept intentionally low to keep the
// service free-forever and prevent abuse. Revisit once the system is stable.
export const MAX_TRACKERS_PER_USER = 20;

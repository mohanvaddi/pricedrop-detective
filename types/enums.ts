export enum SUPPORTED_SITES {
  AMAZON = 'amazon',
  FLIPKART = 'flipkart',
}

export enum BOT_COMMANDS {
  START = 'start',
  HELP = 'HELP',
  CREATE = 'create',
  DELETE = 'delete',
  LIST = 'list',
  HISTORY = 'history',
}
